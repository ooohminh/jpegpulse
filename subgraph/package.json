{
  "name": "jpegpulse-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ jpegpulse",
    "create-local": "graph create --node http://localhost:8020/ jpegpulse",
    "remove-local": "graph remove --node http://localhost:8020/ jpegpulse",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jpegpulse"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}