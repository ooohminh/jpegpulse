{
  "name": "jpegpulse-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio berachain-nft",
    "create-local": "graph create --node http://localhost:8020/ berachain-nft",
    "remove-local": "graph remove --node http://localhost:8020/ berachain-nft",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 berachain-nft"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}